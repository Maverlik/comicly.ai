---
phase: 04-wallet-ledger-and-coin-safety
plan: 03
type: execute
wave: 3
depends_on:
  - 01
  - 02
files_modified:
  - backend/README.md
  - backend/tests/test_wallet_service.py
  - backend/tests/test_wallet_api.py
autonomous: true
requirements:
  - WAL-01
  - WAL-02
  - WAL-03
  - WAL-04
  - WAL-05
  - WAL-06
  - WAL-07
  - TEST-03
must_haves:
  truths:
    - "Backend docs explain wallet ledger semantics and future generation usage."
    - "Full backend pytest and ruff gates pass."
    - "Phase 4 remains backend API-only with no frontend/root runtime file changes."
    - "Wallet docs do not expose secrets or imply client-trusted balances."
  artifacts:
    - path: "backend/README.md"
      provides: "Wallet ledger behavior, idempotency, and future generation integration notes"
    - path: "backend/tests/*wallet*"
      provides: "Complete Phase 4 wallet coverage"
---

<objective>
Finalize wallet ledger docs and full backend quality gates.

Purpose: Make wallet accounting understandable, verified, and ready for Phase 5/6 consumers.
Output: updated backend docs, full test/lint/format pass, and backend-only boundary confirmation.
</objective>

<context>
@.planning/phases/04-wallet-ledger-and-coin-safety/04-CONTEXT.md
@.planning/phases/04-wallet-ledger-and-coin-safety/04-RESEARCH.md
@.planning/phases/04-wallet-ledger-and-coin-safety/04-VALIDATION.md
@.planning/phases/04-wallet-ledger-and-coin-safety/04-01-SUMMARY.md
@.planning/phases/04-wallet-ledger-and-coin-safety/04-02-SUMMARY.md
@backend/BACKEND_TZ.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Document wallet ledger semantics</name>
  <files>backend/README.md</files>
  <behavior>
    - Docs state database wallet balance is authoritative.
    - Docs explain ledger transaction rows for every balance change.
    - Docs document `GET /api/v1/wallet`.
    - Docs document future `Idempotency-Key` requirement for billable operations.
    - Docs note generation costs: 20 coins full page and 4 coins scene regeneration.
    - Docs explain that OpenRouter/generation integration is deferred to Phase 6.
  </behavior>
  <action>Update backend docs with concise wallet accounting guidance and local verification commands.</action>
  <verify>
    <manual>Read docs for consistency with `04-CONTEXT.md` and `backend/BACKEND_TZ.md`.</manual>
  </verify>
  <done>Developers know how wallet safety is intended to work before using it in later phases.</done>
</task>

<task type="auto">
  <name>Task 2: Run full Phase 4 quality and boundary gates</name>
  <files>backend/tests/test_wallet_service.py, backend/tests/test_wallet_api.py</files>
  <behavior>
    - Full pytest suite passes.
    - Ruff lint passes.
    - Ruff format check passes.
    - No frontend/root runtime files are modified.
    - Runtime log files and local secret files are not staged.
  </behavior>
  <action>Run all backend quality gates, fix only backend files if failures are caused by Phase 4 changes, and confirm the root/frontend boundary remains untouched.</action>
  <verify>
    <automated>cd backend; python -m pytest && python -m ruff check . && python -m ruff format --check .</automated>
    <automated>git diff --name-only -- server.js package.json index.html create.html scripts styles.css creator.css</automated>
  </verify>
  <done>Phase 4 backend is ready for code review and verification.</done>
</task>

</tasks>

<verification>
Run from `backend/`:

```powershell
python -m pytest
python -m ruff check .
python -m ruff format --check .
```

Boundary check from repository root:

```powershell
git diff --name-only -- server.js package.json index.html create.html scripts styles.css creator.css
```
</verification>

<success_criteria>
- Wallet ledger docs are accurate and concise.
- All Phase 4 tests and full backend gates pass.
- No frontend/root runtime files are changed.
- Phase 5 can build private comic persistence on authenticated users and authoritative wallets.
</success_criteria>

<output>
After completion, create `.planning/phases/04-wallet-ledger-and-coin-safety/04-03-SUMMARY.md`.
</output>

