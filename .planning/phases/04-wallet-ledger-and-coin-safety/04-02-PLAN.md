---
phase: 04-wallet-ledger-and-coin-safety
plan: 02
type: execute
wave: 2
depends_on:
  - 01
files_modified:
  - backend/app/api/v1/wallet.py
  - backend/app/api/v1/__init__.py
  - backend/tests/test_wallet_api.py
autonomous: true
requirements:
  - WAL-01
  - TEST-03
must_haves:
  truths:
    - "`GET /api/v1/wallet` is authenticated and reads authoritative database state."
    - "Wallet response includes current balance and a short recent transaction list."
    - "Anonymous requests are rejected with the existing auth error envelope."
    - "No public debit endpoint is exposed in Phase 4."
  artifacts:
    - path: "backend/app/api/v1/wallet.py"
      provides: "Authenticated read-only wallet summary endpoint"
    - path: "backend/app/api/v1/__init__.py"
      provides: "Wallet router registration under `/api/v1`"
    - path: "backend/tests/test_wallet_api.py"
      provides: "Wallet API auth, response shape, and transaction ordering tests"
---

<objective>
Expose the authenticated wallet summary API.

Purpose: Give the frontend a future-safe backend endpoint for trusted balance and recent ledger data without exposing mutation endpoints yet.
Output: `GET /api/v1/wallet`, router registration, and API tests.
</objective>

<context>
@.planning/phases/04-wallet-ledger-and-coin-safety/04-CONTEXT.md
@.planning/phases/04-wallet-ledger-and-coin-safety/04-RESEARCH.md
@.planning/phases/04-wallet-ledger-and-coin-safety/04-VALIDATION.md
@.planning/phases/04-wallet-ledger-and-coin-safety/04-01-SUMMARY.md
@backend/BACKEND_TZ.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add read-only wallet route</name>
  <files>backend/app/api/v1/wallet.py, backend/app/api/v1/__init__.py, backend/tests/test_wallet_api.py</files>
  <behavior>
    - `GET /api/v1/wallet` requires the existing current-user dependency.
    - Response includes `balance` and `recent_transactions`.
    - Recent transactions are ordered newest first and limited to a small fixed count.
    - Transaction objects expose audit-safe fields only: id, amount, balance_after, reason, reference fields, and created_at.
  </behavior>
  <action>Create and register the wallet router, delegating reads to the wallet service from Plan 01.</action>
  <verify>
    <automated>cd backend; python -m pytest tests/test_wallet_api.py</automated>
  </verify>
  <done>Authenticated users can read wallet balance and recent ledger entries.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Cover auth and consistency behavior</name>
  <files>backend/tests/test_wallet_api.py, backend/tests/test_wallet_service.py</files>
  <behavior>
    - Anonymous wallet API requests return the existing auth-required response.
    - Authenticated wallet API response balance equals the wallet table balance.
    - Recent transactions reflect wallet service mutations.
    - Duplicate idempotency service calls do not produce duplicate API-visible debits.
  </behavior>
  <action>Add integration-style tests using the existing session/auth fixtures.</action>
  <verify>
    <automated>cd backend; python -m pytest tests/test_wallet_api.py tests/test_wallet_service.py</automated>
  </verify>
  <done>Wallet API proves it is a read surface over authoritative ledger state.</done>
</task>

</tasks>

<verification>
Run from `backend/`:

```powershell
python -m pytest tests/test_wallet_api.py tests/test_wallet_service.py
python -m ruff check .
python -m ruff format --check .
```
</verification>

<success_criteria>
- `GET /api/v1/wallet` exists and is authenticated.
- Response contains authoritative balance plus recent transaction list.
- No public wallet mutation endpoint exists in Phase 4.
- API tests cover anonymous and authenticated behavior.
- No frontend/root runtime files are changed.
</success_criteria>

<output>
After completion, create `.planning/phases/04-wallet-ledger-and-coin-safety/04-02-SUMMARY.md`.
</output>
