---
phase: 02-data-and-payment-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 01
files_modified:
  - backend/alembic/env.py
  - backend/alembic/versions/*.py
  - backend/tests/test_schema_migrations.py
  - backend/tests/test_schema_constraints.py
autonomous: true
requirements:
  - DATA-01
  - DATA-02
  - DATA-03
  - PAY-02
  - PAY-03
must_haves:
  truths:
    - "Alembic can create the full Phase 2 schema from a clean checkout."
    - "Alembic uses the direct migration database URL when configured, while runtime app code keeps using `DATABASE_URL`."
    - "Database constraints reject duplicate provider identities, duplicate idempotency keys, invalid wallet balances, and invalid owner relationships."
    - "Payment placeholder schema can store status, user, package, amount, currency, external provider fields, and future webhook/idempotency identifiers."
  artifacts:
    - path: "backend/alembic/env.py"
      provides: "Migration DB URL selection and imported model metadata"
    - path: "backend/alembic/versions/*.py"
      provides: "Initial production schema migration"
    - path: "backend/tests/test_schema_constraints.py"
      provides: "Representative DB constraint coverage"
    - path: "backend/tests/test_schema_migrations.py"
      provides: "Migration/metadata verification"
  key_links:
    - from: "backend/alembic/env.py"
      to: "backend/app/core/config.py"
      via: "direct migration database URL setting"
      pattern: "direct|migration"
    - from: "backend/alembic/env.py"
      to: "backend/app/models/__init__.py"
      via: "model import for target metadata"
      pattern: "app.models"
---

<objective>
Generate and verify the initial Alembic schema migration with production constraints.

Purpose: Convert the Phase 2 SQLAlchemy metadata into a clean migration path that creates all required backend tables and protects the most important data invariants.
Output: Alembic migration, env.py direct-URL handling, migration tests, and representative constraint tests.
</objective>

<execution_context>
@C:/Users/ivan3/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/ivan3/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-data-and-payment-foundation/02-CONTEXT.md
@.planning/phases/02-data-and-payment-foundation/02-RESEARCH.md
@.planning/phases/02-data-and-payment-foundation/02-VALIDATION.md
@.planning/phases/02-data-and-payment-foundation/02-01-SUMMARY.md
@backend/BACKEND_TZ.md

<locked_decisions>
- Use the models and config created by Plan 01.
- Keep production DB target Neon/Vercel compatible: pooled URL for runtime, direct URL for migrations.
- Do not implement OAuth, wallet debit services, checkout/webhooks, generation calls, or frontend integration.
</locked_decisions>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Update Alembic environment for full metadata and direct migration URL</name>
  <files>backend/alembic/env.py, backend/tests/test_schema_migrations.py</files>
  <behavior>
    - `alembic/env.py` imports model modules before reading `Base.metadata`.
    - If a direct migration DB URL is configured, Alembic uses it instead of runtime `DATABASE_URL`.
    - If no direct URL is configured, Alembic falls back to local/runtime `DATABASE_URL`.
    - Behavior is documented or tested without exposing actual secret values.
  </behavior>
  <action>Modify Alembic setup to import `app.models` and select the migration URL from settings. Add tests that validate metadata includes required tables and that migration URL selection prefers the direct URL when present.</action>
  <verify>
    <automated>cd backend; python -m pytest tests/test_schema_migrations.py</automated>
  </verify>
  <done>Alembic sees all Phase 2 models and can use direct Neon migration URL semantics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create initial production schema migration</name>
  <files>backend/alembic/versions/*.py, backend/tests/test_schema_migrations.py</files>
  <behavior>
    - Migration creates all required tables from DATA-02 and PAY-02.
    - Migration includes indexes, foreign keys, check constraints, uniqueness constraints, and timestamps needed by later phases.
    - Migration can downgrade cleanly enough for local development.
  </behavior>
  <action>Create an Alembic revision for the Phase 2 schema. It may be generated then cleaned up manually. Ensure the migration is explicit and reviewable; do not rely on runtime `create_all()`. Keep table and column names stable and readable.</action>
  <verify>
    <automated>cd backend; python -m alembic upgrade head</automated>
  </verify>
  <done>A clean database can be upgraded to the full Phase 2 schema through Alembic.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add representative schema constraint tests</name>
  <files>backend/tests/test_schema_constraints.py</files>
  <behavior>
    - Duplicate `(provider, provider_user_id)` identities are rejected.
    - Duplicate wallet transaction idempotency keys are rejected.
    - Negative wallet balances are rejected.
    - Duplicate payment/provider idempotency identifiers are rejected where applicable.
    - Cross-owner or orphan-prone comic/page relationships are constrained by foreign keys.
  </behavior>
  <action>Add focused tests for the highest-risk constraints. Prefer real database integration against local test DB when available; if the project test harness cannot rely on Docker, add metadata/DDL assertions plus document the integration command in the test/doc output. Do not mock away constraint definitions.</action>
  <verify>
    <automated>cd backend; python -m pytest tests/test_schema_constraints.py</automated>
  </verify>
  <done>Representative constraint coverage proves DATA-03/PAY-03 protections exist.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Alembic to production database | Migration tooling can mutate production schema. |
| application data to relational constraints | Untrusted future API inputs will be persisted only if constraints allow them. |
| payment provider future webhooks to DB | Future external events need idempotency protections. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2-04 | T | `backend/alembic/env.py` | mitigate | Prefer direct migration URL only inside migration tooling; keep runtime pooled URL separate. |
| T-2-05 | T | provider identities | mitigate | Unique `(provider, provider_user_id)` constraint. |
| T-2-06 | T | wallet transactions | mitigate | Unique idempotency keys and non-negative wallet balance checks. |
| T-2-07 | R | payments | mitigate | Store provider/idempotency identifiers and payment statuses for future audit. |
</threat_model>

<verification>
Run from `backend/`:

```powershell
python -m alembic upgrade head
python -m pytest tests/test_schema_migrations.py tests/test_schema_constraints.py
python -m ruff check .
python -m ruff format --check .
```
</verification>

<success_criteria>
- Alembic creates every required Phase 2 table.
- Direct migration DB URL behavior exists and is tested/documented.
- Required uniqueness, check, and foreign-key constraints exist.
- Payment placeholder schema supports future provider webhook idempotency.
- No deferred business behavior is implemented.
</success_criteria>

<output>
After completion, create `.planning/phases/02-data-and-payment-foundation/02-02-SUMMARY.md`.
</output>
