---
phase: 02-data-and-payment-foundation
plan: 03
type: execute
wave: 3
depends_on:
  - 01
  - 02
files_modified:
  - backend/app/api/__init__.py
  - backend/app/api/v1/__init__.py
  - backend/app/api/v1/coin_packages.py
  - backend/app/main.py
  - backend/app/services/__init__.py
  - backend/app/services/coin_packages.py
  - backend/scripts/seed_coin_packages.py
  - backend/tests/test_coin_packages.py
  - backend/tests/test_config.py
  - backend/README.md
  - backend/.env.example
autonomous: true
requirements:
  - DATA-04
  - DATA-05
  - PAY-01
must_haves:
  truths:
    - "Developer can seed active 100, 500, and 1000 coin packages idempotently."
    - "Backend can return the active coin package catalog from `/api/v1/...` without implementing checkout."
    - "Generation costs and starter coins are centralized in runtime configuration and covered by tests/docs."
    - "Backend docs explain local Docker Postgres, Neon pooled runtime URL, direct migration URL, seed, migration, and verification commands."
  artifacts:
    - path: "backend/scripts/seed_coin_packages.py"
      provides: "Idempotent coin package seed command"
    - path: "backend/app/api/v1/coin_packages.py"
      provides: "Active package catalog API"
    - path: "backend/app/services/coin_packages.py"
      provides: "Coin package read/seed helpers"
    - path: "backend/tests/test_coin_packages.py"
      provides: "Seed/catalog API coverage"
    - path: "backend/README.md"
      provides: "Phase 2 migration, Neon, env, and seed docs"
  key_links:
    - from: "backend/app/main.py"
      to: "backend/app/api/v1/coin_packages.py"
      via: "router include"
      pattern: "/api/v1"
    - from: "backend/scripts/seed_coin_packages.py"
      to: "backend/app/models/payment.py"
      via: "coin package model"
      pattern: "CoinPackage"
---

<objective>
Add coin package seeding, package catalog API, pricing docs/tests, and Phase 2 quality gates.

Purpose: Complete the payment-preparation surface required for MVP planning without implementing real checkout or wallet fulfillment.
Output: idempotent seed path, active package catalog endpoint, tests, and updated backend documentation.
</objective>

<execution_context>
@C:/Users/ivan3/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/ivan3/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-data-and-payment-foundation/02-CONTEXT.md
@.planning/phases/02-data-and-payment-foundation/02-RESEARCH.md
@.planning/phases/02-data-and-payment-foundation/02-VALIDATION.md
@.planning/phases/02-data-and-payment-foundation/02-01-SUMMARY.md
@.planning/phases/02-data-and-payment-foundation/02-02-SUMMARY.md
@backend/BACKEND_TZ.md

<locked_decisions>
- Business APIs added in this phase must be under `/api/v1/...`.
- Add only package catalog read behavior, not checkout, webhooks, wallet crediting, or authenticated payment flows.
- Keep deployment execution deferred; document Vercel/Neon requirements only.
</locked_decisions>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add idempotent coin package seed path</name>
  <files>backend/scripts/seed_coin_packages.py, backend/app/services/coin_packages.py, backend/tests/test_coin_packages.py</files>
  <behavior>
    - Running the seed once creates active 100, 500, and 1000 coin packages.
    - Running the seed again does not create duplicates.
    - Package metadata includes stable code/name, coin amount, amount/currency fields appropriate for future checkout, active flag, and timestamps.
  </behavior>
  <action>Create a simple backend-local seed script or callable service that uses async SQLAlchemy sessions. Keep it easy to invoke from `backend/` with Python. Add tests proving idempotency and the three required packages.</action>
  <verify>
    <automated>cd backend; python -m pytest tests/test_coin_packages.py</automated>
  </verify>
  <done>Coin package seed creates exactly the required active package catalog idempotently.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add active coin package catalog endpoint</name>
  <files>backend/app/api/v1/__init__.py, backend/app/api/v1/coin_packages.py, backend/app/api/__init__.py, backend/app/main.py, backend/app/services/coin_packages.py, backend/tests/test_coin_packages.py</files>
  <behavior>
    - `GET /api/v1/coin-packages` or similarly clear route returns active packages only.
    - Response includes stable id/code, coin amount, display name, amount/currency, and active status if useful.
    - Route does not create payments and does not require frontend changes.
    - Route uses existing API error conventions.
  </behavior>
  <action>Add a minimal `/api/v1` router and coin package catalog route. Keep the response small and stable for later frontend integration. Do not add auth requirements in Phase 2 unless represented as a no-op future placeholder; Phase 3 owns auth.</action>
  <verify>
    <automated>cd backend; python -m pytest tests/test_coin_packages.py</automated>
  </verify>
  <done>Active package catalog API satisfies PAY-01 without implementing real checkout.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Finalize pricing/env docs and full quality gates</name>
  <files>backend/README.md, backend/.env.example, backend/tests/test_config.py</files>
  <behavior>
    - README documents local migration commands, seed command, local Docker DB, production Neon pooled `DATABASE_URL`, direct migration URL, and Vercel-first deployment constraints.
    - `.env.example` documents `DATABASE_URL`, direct migration URL, `CORS_ORIGINS`, pricing vars, `STARTER_COINS`, and future secret names without real values.
    - Tests prove default costs remain 20 and 4 unless env overrides are supplied.
  </behavior>
  <action>Update backend docs and env example for Phase 2. Include warnings that Docker Compose is local-only and production DB should be managed Postgres/Neon. Run and fix full backend quality gates.</action>
  <verify>
    <automated>cd backend; python -m pytest && python -m ruff check . && python -m ruff format --check .</automated>
  </verify>
  <done>Docs, env examples, tests, lint, and format are green for the complete Phase 2 foundation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| anonymous client to package catalog | Public users can fetch package data before auth is implemented. |
| seed script to database | Developer command writes catalog rows. |
| environment to pricing config | Runtime pricing values affect future billing behavior. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2-08 | T | `seed_coin_packages.py` | mitigate | Make seed idempotent using stable package codes/constraints. |
| T-2-09 | I | `/api/v1/coin-packages` | mitigate | Return only public catalog fields; no payment provider secrets or env values. |
| T-2-10 | T | pricing settings | mitigate | Centralize defaults and env overrides, with tests for 20/4 generation costs and starter coins. |
</threat_model>

<verification>
Run from `backend/`:

```powershell
python -m pytest
python -m ruff check .
python -m ruff format --check .
```

When running as one command, use fail-fast chaining:

```powershell
python -m pytest && python -m ruff check . && python -m ruff format --check .
```

Optional local integration check when Docker Desktop is running:

```powershell
docker compose up -d
python -m alembic upgrade head
python scripts/seed_coin_packages.py
```
</verification>

<success_criteria>
- Active 100/500/1000 coin packages can be seeded idempotently.
- Active package catalog can be fetched under `/api/v1/...`.
- Pricing and starter coin config are centralized and tested.
- Backend docs cover local Docker DB, Neon pooled runtime URL, direct migration URL, migrations, seed, and quality gates.
- No real checkout, webhook fulfillment, wallet credit/debit, OpenRouter call, or frontend integration is implemented.
</success_criteria>

<output>
After completion, create `.planning/phases/02-data-and-payment-foundation/02-03-SUMMARY.md`.
</output>
