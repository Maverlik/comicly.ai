---
phase: 02-data-and-payment-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/core/config.py
  - backend/app/db/session.py
  - backend/app/models/__init__.py
  - backend/app/models/user.py
  - backend/app/models/wallet.py
  - backend/app/models/comic.py
  - backend/app/models/generation.py
  - backend/app/models/payment.py
  - backend/tests/test_config.py
  - backend/tests/test_models.py
  - backend/.env.example
autonomous: true
requirements:
  - DATA-02
  - DATA-05
must_haves:
  truths:
    - "Runtime app config has a pooled `DATABASE_URL` path and Alembic can use a separate direct migration URL when configured."
    - "Generation pricing and starter coins are centralized in backend settings, not scattered through route code or frontend files."
    - "SQLAlchemy metadata includes all Phase 2 table models needed by later auth, wallet, comic, generation, and payment phases."
    - "Model modules are imported so Alembic autogenerate sees the full metadata."
  artifacts:
    - path: "backend/app/core/config.py"
      provides: "Vercel/Neon-aware DB and pricing settings"
      exports: ["Settings", "get_settings"]
    - path: "backend/app/db/session.py"
      provides: "Serverless-friendly async engine/session setup"
      exports: ["engine", "async_session_maker"]
    - path: "backend/app/models/__init__.py"
      provides: "Model import surface for Alembic metadata"
    - path: "backend/tests/test_models.py"
      provides: "Required table metadata coverage"
  key_links:
    - from: "backend/alembic/env.py"
      to: "backend/app/models/__init__.py"
      via: "metadata model import"
      pattern: "app.models"
    - from: "backend/app/db/session.py"
      to: "backend/app/core/config.py"
      via: "settings-driven database URL"
      pattern: "get_settings"
---

<objective>
Create the Phase 2 configuration and SQLAlchemy model foundation.

Purpose: Prepare the backend schema surface and serverless-friendly database configuration before generating migrations and seed/catalog behavior.
Output: expanded settings, updated session engine behavior, model modules for all required tables, and metadata/config tests.
</objective>

<execution_context>
@C:/Users/ivan3/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/ivan3/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-data-and-payment-foundation/02-CONTEXT.md
@.planning/phases/02-data-and-payment-foundation/02-RESEARCH.md
@.planning/phases/02-data-and-payment-foundation/02-VALIDATION.md
@.planning/phases/01-backend-foundation-and-static-safety/01-CONTEXT.md
@.planning/phases/01-backend-foundation-and-static-safety/01-VERIFICATION.md
@backend/BACKEND_TZ.md

<locked_decisions>
- Modify only `backend/` runtime files plus phase planning docs unless user explicitly approves otherwise.
- Backend remains standalone API-only FastAPI under `backend/`.
- Production target is Vercel-first with separate backend project rooted at `backend/`.
- Runtime `DATABASE_URL` should be Neon pooled URL in production; migrations may use a separate direct URL.
- Local database remains Docker Compose Postgres.
- Phase 2 implements schema/config foundation only: no OAuth flow, no wallet debits, no OpenRouter calls, no real checkout/webhooks, no frontend integration.
</locked_decisions>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Expand settings for database URLs and pricing</name>
  <files>backend/app/core/config.py, backend/.env.example, backend/tests/test_config.py</files>
  <behavior>
    - `Settings.database_url` remains the runtime URL and can be a Neon pooled URL in production.
    - A separate direct migration URL setting exists, using either `DATABASE_DIRECT_URL` or `MIGRATION_DATABASE_URL`.
    - Alembic can prefer the direct migration URL later without changing runtime app code.
    - `full_page_generation_cost`, `scene_regeneration_cost`, and `starter_coins` have safe numeric defaults and env overrides.
    - Future env names such as `CORS_ORIGINS`, OAuth secrets, `OPENROUTER_API_KEY`, and storage vars are documented without requiring them at startup.
  </behavior>
  <action>Update `Settings` with direct migration database URL support, CORS origin placeholder/config, and centralized pricing/starter coin fields. Keep Phase 1 safe startup behavior: missing future secrets must not prevent import. Update backend `.env.example` with local Docker values and production comments for Neon pooled/direct URLs.</action>
  <verify>
    <automated>cd backend; python -m pytest tests/test_config.py</automated>
  </verify>
  <done>Config tests prove pricing defaults, env overrides, runtime DB URL, and direct migration URL behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add required SQLAlchemy model modules</name>
  <files>backend/app/models/__init__.py, backend/app/models/user.py, backend/app/models/wallet.py, backend/app/models/comic.py, backend/app/models/generation.py, backend/app/models/payment.py, backend/tests/test_models.py</files>
  <behavior>
    - `Base.metadata.tables` includes users, provider identities, profiles, sessions, wallets, wallet transactions, comics, comic scenes, comic pages, generation jobs, coin packages, and payments.
    - Models include columns needed by later phases for ownership, status, timestamps, provider ids, payment provider fields, image/page metadata, and idempotency.
    - Model definitions do not implement auth, debit, generation, or checkout behavior.
    - Importing `app.models` registers all models with `Base.metadata`.
  </behavior>
  <action>Create model modules grouped by domain. Use SQLAlchemy 2 typed declarative style. Prefer UUID primary keys for user-owned/business records, timezone-aware timestamp columns, explicit foreign keys, and simple enum/string status columns that are easy for Alembic to migrate. Add `backend/tests/test_models.py` asserting required tables and representative columns exist in metadata.</action>
  <verify>
    <automated>cd backend; python -m pytest tests/test_models.py</automated>
  </verify>
  <done>All required Phase 2 tables are represented in SQLAlchemy metadata and imported for Alembic.</done>
</task>

<task type="auto">
  <name>Task 3: Make async session setup serverless-friendly</name>
  <files>backend/app/db/session.py, backend/tests/test_config.py</files>
  <behavior>
    - Runtime engine still works locally with Docker Postgres.
    - Engine setup is conservative for serverless pooled URLs: no unbounded pool assumptions.
    - Sessionmaker remains importable as `async_session_maker`.
    - Tests or assertions prove runtime engine creation uses `Settings.database_url` and does not accidentally use the direct migration URL.
  </behavior>
  <action>Review `create_async_engine` settings for Vercel/Neon pooled runtime. Keep it simple and portable; use settings-driven pool options only if needed. Add a focused test or helper assertion proving runtime app engine construction uses `DATABASE_URL`, while direct migration URL remains reserved for Alembic. Do not introduce Vercel-only code into business modules.</action>
  <verify>
    <automated>cd backend; python -m pytest tests/test_config.py</automated>
  </verify>
  <done>DB session infrastructure remains importable, uses the runtime pooled URL for app execution, and leaves direct URL usage to Alembic.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| environment to app config | Production secrets and DB URLs enter app runtime through env vars. |
| serverless runtime to Neon | Many short-lived function instances can open database connections. |
| model metadata to migrations | Alembic will turn model definitions into production schema changes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2-01 | I | `backend/app/core/config.py` | mitigate | Do not print or expose DB URLs/secrets; only document env names and load values from env. |
| T-2-02 | D | `backend/app/db/session.py` | mitigate | Keep runtime engine compatible with pooled Neon URL and avoid aggressive connection pool settings. |
| T-2-03 | T | `backend/app/models/*` | mitigate | Add model metadata tests so missing tables/columns are caught before migration generation. |
</threat_model>

<verification>
Run from `backend/`:

```powershell
python -m pytest tests/test_config.py tests/test_models.py
python -c "from app.db.session import engine, async_session_maker; assert engine is not None; assert async_session_maker is not None"
python -m ruff check .
python -m ruff format --check .
```
</verification>

<success_criteria>
- Only `backend/` runtime files are modified by this plan.
- Settings support pooled runtime DB URL and separate direct migration DB URL.
- Pricing and starter coins are centralized in settings.
- SQLAlchemy metadata includes all required Phase 2 table models.
- Future business behavior remains deferred.
</success_criteria>

<output>
After completion, create `.planning/phases/02-data-and-payment-foundation/02-01-SUMMARY.md`.
</output>
