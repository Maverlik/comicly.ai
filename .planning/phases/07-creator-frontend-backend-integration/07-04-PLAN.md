---
phase: 07-creator-frontend-backend-integration
plan: 04
type: execute
wave: 4
depends_on:
  - 01
  - 02
  - 03
files_modified:
  - scripts/creator.js
  - create.html
  - creator.css
  - tests/phase7-static-contract.test.mjs
  - .planning/phases/07-creator-frontend-backend-integration/07-VERIFICATION.md
  - .planning/phases/07-creator-frontend-backend-integration/07-REVIEW.md
autonomous: true
requirements:
  - PROF-04
  - TEST-06
must_haves:
  truths:
    - "Logout calls backend logout and returns creator to unauthenticated overlay."
    - "Landing remains public."
    - "Phase 7 smoke covers auth gate, session/profile/balance, generation success/errors, and logout."
    - "Full backend gates and frontend/static checks pass."
  artifacts:
    - path: ".planning/phases/07-creator-frontend-backend-integration/07-VERIFICATION.md"
      provides: "Phase 7 verification evidence"
---

<objective>
Finalize creator backend integration with logout and end-to-end verification.

Purpose: Prove the integrated creator works as an authenticated backend-backed MVP without breaking landing.
Output: logout integration, browser/static/backend verification, review, and verification docs.
</objective>

<context>
@.planning/phases/07-creator-frontend-backend-integration/07-CONTEXT.md
@.planning/phases/07-creator-frontend-backend-integration/07-UI-SPEC.md
@.planning/phases/07-creator-frontend-backend-integration/07-VALIDATION.md
@.planning/phases/07-creator-frontend-backend-integration/07-01-SUMMARY.md
@.planning/phases/07-creator-frontend-backend-integration/07-02-SUMMARY.md
@.planning/phases/07-creator-frontend-backend-integration/07-03-SUMMARY.md
@backend/BACKEND_TZ.md
</context>

<threat_model>
- **Threat:** Logout only clears UI and leaves server session active. **Mitigation:** Call `POST /api/v1/me/logout` and then show unauth overlay.
- **Threat:** Verification misses landing regression. **Mitigation:** Browser/static smoke explicitly checks public landing.
- **Threat:** OAuth provider flow cannot be automated. **Mitigation:** Verify login targets and session bootstrap automatically; document any live-provider manual gap.
- **Threat:** Frontend exposes secrets. **Mitigation:** Static contract check scans for provider key patterns and no `OPENROUTER_API_KEY` frontend usage.
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace demo logout/profile/add credits actions</name>
  <files>scripts/creator.js, create.html, creator.css, tests/phase7-static-contract.test.mjs</files>
  <behavior>
    - Logout calls `POST /api/v1/me/logout`.
    - Successful logout clears trusted frontend state and shows auth overlay.
    - Fake add-credits action is removed, hidden, disabled, or made clearly unavailable for MVP.
    - Fake profile/demo menu messages are replaced with backend-backed or unavailable states.
  </behavior>
  <action>Remove remaining production demo-truth behavior from profile/credits controls.</action>
  <verify>
    <automated>node --test tests/phase7-static-contract.test.mjs</automated>
  </verify>
  <done>Profile/balance/logout controls reflect backend truth.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Run browser and backend smoke checks</name>
  <files>none</files>
  <behavior>
    - Landing remains public.
    - Creator without session shows login overlay.
    - Authenticated local/session setup shows profile and balance.
    - Generation loading/success/error states are exercised with live or controlled backend behavior.
    - Logout returns to auth overlay.
  </behavior>
  <action>Start needed local services and run browser smoke using the available browser/Playwright tooling.</action>
  <verify>
    <manual>Record exact URLs, scenarios, and observed results in 07-VERIFICATION.md.</manual>
  </verify>
  <done>Phase 7 user-facing flow has smoke evidence.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Run gates, review, and verification docs</name>
  <files>.planning/phases/07-creator-frontend-backend-integration/07-VERIFICATION.md, .planning/phases/07-creator-frontend-backend-integration/07-REVIEW.md</files>
  <behavior>
    - `node --test tests/phase7-static-contract.test.mjs` passes.
    - Full backend pytest/ruff gates pass.
    - Review checks auth gate, endpoint migration, strict backend truth, idempotency, error handling, no history UI, no landing regression, and no secret leakage.
    - Verification maps `PROF-04` and `TEST-06` to evidence.
  </behavior>
  <action>Run quality gates and create concise review/verification artifacts.</action>
  <verify>
    <automated>node --test tests/phase7-static-contract.test.mjs</automated>
    <automated>cd backend; python -m pytest</automated>
    <automated>cd backend; python -m ruff check .</automated>
    <automated>cd backend; python -m ruff format --check .</automated>
  </verify>
  <done>Phase 7 is ready for user verification and Phase 8 discussion.</done>
</task>

</tasks>

<verification>
Run from repository root:

```powershell
node --test tests/phase7-static-contract.test.mjs
```

Run from `backend/`:

```powershell
python -m pytest
python -m ruff check .
python -m ruff format --check .
```
</verification>

<success_criteria>
- Logout is backend-backed.
- Landing remains public.
- Creator requires auth and uses backend profile/balance/generation truth.
- Required smoke scenarios are recorded.
- No frontend secret leakage or old root AI endpoint usage remains.
</success_criteria>

<output>
After completion, create `.planning/phases/07-creator-frontend-backend-integration/07-04-SUMMARY.md`.
</output>
