---
phase: 06-production-ai-generation-pipeline
plan: 05
type: execute
wave: 5
depends_on:
  - 01
  - 02
  - 03
  - 04
files_modified:
  - backend/README.md
  - backend/.env.example
  - backend/tests/test_openrouter_service.py
  - backend/tests/test_generation_service.py
  - backend/tests/test_generations_api.py
  - backend/tests/test_ai_text_api.py
autonomous: true
requirements:
  - GEN-01
  - GEN-02
  - GEN-03
  - GEN-04
  - GEN-05
  - GEN-06
  - GEN-07
  - TEST-05
must_haves:
  truths:
    - "Backend docs explain generation endpoints, env vars, Blob storage, idempotency, and sync MVP limits."
    - "Full backend pytest and ruff gates pass."
    - "Phase 6 remains backend API-only with no frontend/root runtime file changes."
    - "Docs make clear async polling/queue is deferred."
  artifacts:
    - path: "backend/README.md"
      provides: "Phase 6 generation, AI text, storage, and idempotency documentation"
    - path: "backend/.env.example"
      provides: "Phase 6 env reference for OpenRouter, model allow-list, timeout, and Blob token"
---

<objective>
Finalize Phase 6 documentation, quality gates, and backend-only verification.

Purpose: Make the generation pipeline understandable and prove it satisfies every Phase 6 requirement before frontend integration begins.
Output: docs update, full backend gates, boundary confirmation, review/verification artifacts.
</objective>

<context>
@.planning/phases/06-production-ai-generation-pipeline/06-CONTEXT.md
@.planning/phases/06-production-ai-generation-pipeline/06-RESEARCH.md
@.planning/phases/06-production-ai-generation-pipeline/06-VALIDATION.md
@.planning/phases/06-production-ai-generation-pipeline/06-01-SUMMARY.md
@.planning/phases/06-production-ai-generation-pipeline/06-02-SUMMARY.md
@.planning/phases/06-production-ai-generation-pipeline/06-03-SUMMARY.md
@.planning/phases/06-production-ai-generation-pipeline/06-04-SUMMARY.md
@backend/BACKEND_TZ.md
</context>

<threat_model>
- **Threat:** Operators deploy without required OpenRouter/Blob env vars. **Mitigation:** Document required env vars and local/prod notes.
- **Threat:** Future frontend assumes async queue or legacy route replacement already exists. **Mitigation:** Document synchronous MVP and Phase 7 boundary clearly.
- **Threat:** Regression in auth/wallet/comic behavior from generation changes. **Mitigation:** Run full backend pytest and ruff gates.
- **Threat:** Accidental frontend/root edits. **Mitigation:** Run explicit boundary diff check and stage only approved files.
</threat_model>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Document generation pipeline</name>
  <files>backend/README.md, backend/.env.example</files>
  <behavior>
    - Docs list generation and AI text endpoints.
    - Docs explain `Idempotency-Key`, model allow-list, `MODEL_NOT_ALLOWED`, debit/refund behavior, Vercel Blob storage, and no-base64 response rule.
    - Docs explain synchronous MVP and deferred polling/queue path.
    - Env example documents all Phase 6 env vars without secrets.
  </behavior>
  <action>Update backend docs and env example with concise Phase 6 operational notes.</action>
  <verify>
    <manual>Read docs against `06-CONTEXT.md`, `06-VALIDATION.md`, and `backend/BACKEND_TZ.md`.</manual>
  </verify>
  <done>Future agents and developers can operate the Phase 6 backend without reading all implementation code.</done>
</task>

<task type="auto">
  <name>Task 2: Run full backend gates and boundary check</name>
  <files>backend/tests/test_openrouter_service.py, backend/tests/test_generation_service.py, backend/tests/test_generations_api.py, backend/tests/test_ai_text_api.py</files>
  <behavior>
    - Full pytest suite passes.
    - Ruff lint passes.
    - Ruff format check passes.
    - No frontend/root runtime files are modified.
  </behavior>
  <action>Run full backend quality gates, fix only backend files if failures are caused by Phase 6 changes, and confirm frontend/root boundary remains untouched.</action>
  <verify>
    <automated>cd backend; python -m pytest && python -m ruff check . && python -m ruff format --check .</automated>
    <automated>git diff --name-only -- server.js package.json index.html create.html scripts styles.css creator.css .env.example</automated>
  </verify>
  <done>Phase 6 is ready for code review and verification.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Create review and verification artifacts</name>
  <files>.planning/phases/06-production-ai-generation-pipeline/06-REVIEW.md, .planning/phases/06-production-ai-generation-pipeline/06-VERIFICATION.md</files>
  <behavior>
    - Review checks for double-charge, missing refund, owner-scope, secret leakage, base64 response, and frontend/root boundary issues.
    - Verification maps GEN-01..GEN-07 and TEST-05 to implemented code/tests.
  </behavior>
  <action>Create concise review and verification docs after gates pass.</action>
  <verify>
    <manual>Confirm every Phase 6 success criterion is represented in tests or docs.</manual>
  </verify>
  <done>Phase 6 can be marked complete after execution verification.</done>
</task>

</tasks>

<verification>
Run from `backend/`:

```powershell
python -m pytest
python -m ruff check .
python -m ruff format --check .
```

Boundary check from repository root:

```powershell
git diff --name-only -- server.js package.json index.html create.html scripts styles.css creator.css .env.example
```
</verification>

<success_criteria>
- Backend docs and env example describe Phase 6 behavior accurately.
- All Phase 6 tests and full backend gates pass.
- No frontend/root runtime files are changed.
- Review/verification artifacts are ready for phase completion.
</success_criteria>

<output>
After completion, create `.planning/phases/06-production-ai-generation-pipeline/06-05-SUMMARY.md`.
</output>
