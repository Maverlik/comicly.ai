---
phase: 06-production-ai-generation-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/core/config.py
  - backend/app/models/generation.py
  - backend/alembic/versions/0003_phase6_generation_idempotency.py
  - backend/.env.example
  - backend/requirements.txt
  - backend/requirements-runtime.txt
  - backend/tests/test_config.py
  - backend/tests/test_models.py
  - backend/tests/test_schema_migrations.py
autonomous: true
requirements:
  - GEN-01
  - GEN-03
  - GEN-05
must_haves:
  truths:
    - "Runtime config exposes OpenRouter, model allow-list, timeout, and Vercel Blob settings without requiring secrets at import/startup."
    - "Generation jobs can be looked up/replayed by idempotency key."
    - "Vercel Blob Python dependency is available in runtime requirements."
    - "Schema/config changes remain backend-only."
  artifacts:
    - path: "backend/app/core/config.py"
      provides: "Phase 6 OpenRouter, model policy, timeout, and Blob settings"
    - path: "backend/app/models/generation.py"
      provides: "Generation job idempotency metadata"
    - path: "backend/alembic/versions/0003_phase6_generation_idempotency.py"
      provides: "Migration for generation job idempotency"
---

<objective>
Prepare Phase 6 configuration, dependencies, and generation job schema.

Purpose: Give later plans stable settings and idempotency persistence before adding provider calls or API routes.
Output: config/env updates, Vercel Blob dependency, generation job idempotency migration, and schema/config tests.
</objective>

<context>
@.planning/phases/06-production-ai-generation-pipeline/06-CONTEXT.md
@.planning/phases/06-production-ai-generation-pipeline/06-RESEARCH.md
@.planning/phases/06-production-ai-generation-pipeline/06-VALIDATION.md
@backend/BACKEND_TZ.md
</context>

<threat_model>
- **Threat:** Client can select unsafe/expensive arbitrary models. **Mitigation:** Add server-side allow-list settings and later reject disallowed `model_id` with `MODEL_NOT_ALLOWED`.
- **Threat:** Retry/double click creates multiple generation jobs and debits. **Mitigation:** Persist a generation idempotency key and cover it in metadata/migration tests.
- **Threat:** Secrets leak through committed config. **Mitigation:** Document env names only; do not commit real keys or tokens.
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Phase 6 runtime settings</name>
  <files>backend/app/core/config.py, backend/tests/test_config.py, backend/.env.example</files>
  <behavior>
    - Settings include `OPENROUTER_API_KEY`, `OPENROUTER_SITE_URL`, `OPENROUTER_APP_NAME`, default image/text models, allowed image model list, image aspect ratio/size, request timeout, and `BLOB_READ_WRITE_TOKEN`.
    - Missing OpenRouter/Blob secrets do not break app import, health checks, or tests.
    - Allowed image models parse from comma-separated env into a trimmed list/set.
    - `.env.example` documents production/local values without secrets.
  </behavior>
  <action>Extend settings and config tests for Phase 6 env behavior.</action>
  <verify>
    <automated>cd backend; python -m pytest tests/test_config.py</automated>
  </verify>
  <done>Generation/storage configuration is explicit and safe to import without live secrets.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add generation job idempotency schema</name>
  <files>backend/app/models/generation.py, backend/alembic/versions/0003_phase6_generation_idempotency.py, backend/tests/test_models.py, backend/tests/test_schema_migrations.py</files>
  <behavior>
    - `GenerationJob` has an `idempotency_key` column.
    - Migration adds and downgrades the column plus a uniqueness constraint or index.
    - Tests prove metadata and migration mention the idempotency field.
  </behavior>
  <action>Update the ORM and Alembic migration for generation idempotency replay.</action>
  <verify>
    <automated>cd backend; python -m pytest tests/test_models.py tests/test_schema_migrations.py</automated>
  </verify>
  <done>Generation jobs can support retry-safe request replay.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Add Vercel Blob runtime dependency</name>
  <files>backend/requirements.txt, backend/requirements-runtime.txt</files>
  <behavior>
    - Runtime requirements include the official Python package needed for `vercel.blob.AsyncBlobClient`.
    - Development requirements remain a superset of runtime requirements.
  </behavior>
  <action>Add the Vercel package dependency in the same pinned style as existing requirements.</action>
  <verify>
    <automated>cd backend; python -m ruff check app/core/config.py app/models/generation.py tests/test_config.py tests/test_models.py tests/test_schema_migrations.py</automated>
  </verify>
  <done>Later storage service can import the Blob client in production runtime.</done>
</task>

</tasks>

<verification>
Run from `backend/`:

```powershell
python -m pytest tests/test_config.py tests/test_models.py tests/test_schema_migrations.py
python -m ruff check .
python -m ruff format --check .
```
</verification>

<success_criteria>
- Phase 6 settings exist and are documented.
- Generation job idempotency field exists in model and migration.
- Runtime dependency for Vercel Blob exists.
- No frontend/root runtime files are changed.
</success_criteria>

<output>
After completion, create `.planning/phases/06-production-ai-generation-pipeline/06-01-SUMMARY.md`.
</output>
