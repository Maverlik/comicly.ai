---
phase: 08-deployment-and-operations
plan: 04
type: execute
wave: 4
depends_on:
  - 01
  - 02
  - 03
files_modified:
  - .planning/phases/08-deployment-and-operations/08-REVIEW.md
  - .planning/phases/08-deployment-and-operations/08-VERIFICATION.md
  - backend/docs/deployment.md
autonomous: true
requirements:
  - OPS-01
  - OPS-02
  - OPS-03
  - OPS-04
  - OPS-05
must_haves:
  truths:
    - "Execution attempts Vercel deploy/setup through available tools/CLI when access is available."
    - "If Vercel/project/domain/secrets block deployment, the exact blocker and manual next action are recorded."
    - "Local backend, root/static, Docker config, and documentation gates pass before Phase 8 completion."
    - "Final verification maps every OPS requirement to implemented config/docs/checks or explicit external manual gaps."
  artifacts:
    - path: ".planning/phases/08-deployment-and-operations/08-REVIEW.md"
      provides: "Deployment/security review findings"
    - path: ".planning/phases/08-deployment-and-operations/08-VERIFICATION.md"
      provides: "Final Phase 8 verification status and deployment smoke result"
---

<objective>
Attempt deployment-oriented verification and close Phase 8 with honest operational status.

Purpose: Run the full local gates, try Vercel setup/deploy/smoke within available access, and record whether production is live or which external credentials/domain/provider steps remain.
Output: deployment attempt notes, review, verification, and ready-to-execute/ready-to-deploy status.
</objective>

<context>
@.planning/phases/08-deployment-and-operations/08-CONTEXT.md
@.planning/phases/08-deployment-and-operations/08-RESEARCH.md
@.planning/phases/08-deployment-and-operations/08-VALIDATION.md
@.planning/phases/08-deployment-and-operations/08-01-SUMMARY.md
@.planning/phases/08-deployment-and-operations/08-02-SUMMARY.md
@.planning/phases/08-deployment-and-operations/08-03-SUMMARY.md
@backend/BACKEND_TZ.md
</context>

<threat_model>
- **T-08-13 Threat:** Phase is marked complete even though deployment was not actually attempted. **Mitigation:** Execute Vercel CLI/tool checks and record results or blockers.
- **T-08-14 Threat:** Secrets appear in logs, docs, or commits during deploy work. **Mitigation:** Do not echo env values; redact any command output before writing verification docs.
- **T-08-15 Threat:** Production smoke passes public endpoints but misses broken OAuth/generation flows. **Mitigation:** Verification separates automated public checks from live provider checks and records manual gaps.
- **T-08-16 Threat:** Deployment config passes locally but root/static or backend gates regress. **Mitigation:** Run full local gates before final verification.
</threat_model>

<tasks>

<task type="manual" tdd="false">
  <name>Task 1: Run full local readiness gates</name>
  <files>none</files>
  <behavior>
    - Backend pytest passes.
    - Backend ruff lint and format pass.
    - Docker Compose config validates.
    - Root static contract/deploy-config tests pass.
    - Frontend build output is generated and inspected for private-file exclusions.
  </behavior>
  <action>Run all local gates and fix only files within Phase 8 scope if failures are caused by Phase 8 changes.</action>
  <verify>
    <automated>cd backend; python -m pytest; python -m ruff check .; python -m ruff format --check .; docker compose config</automated>
    <automated>node --test tests/phase7-static-contract.test.mjs tests/phase8-deploy-config.test.mjs</automated>
    <automated>npm run build:frontend</automated>
  </verify>
  <done>Local readiness is green before deploy attempt.</done>
</task>

<task type="manual" tdd="false">
  <name>Task 2: Attempt Vercel project/deploy setup</name>
  <files>backend/docs/deployment.md</files>
  <behavior>
    - Check Vercel CLI/tool authentication without printing secrets.
    - Attempt or document frontend project linkage/deploy for the root static project.
    - Attempt or document backend project linkage/deploy rooted at `backend/`.
    - Use Vercel env/Marketplace values where available; do not commit local `.env` or secrets.
    - If access is blocked, write the exact blocking step and manual action in deployment docs or verification.
  </behavior>
  <action>Use available Vercel tools/CLI to get as far as possible toward the agreed production deploy.</action>
  <verify>
    <manual>Record Vercel project names/URLs or exact blocker without secret values.</manual>
  </verify>
  <done>Deployment has either been attempted successfully or has precise external blockers recorded.</done>
</task>

<task type="manual" tdd="false">
  <name>Task 3: Run smoke checks against available URLs</name>
  <files>none</files>
  <behavior>
    - If production URLs are available, run smoke helper against `https://comicly.ai` and `https://api.comicly.ai`.
    - If only preview URLs are available, run smoke helper against those and record preview status.
    - If no deployed URLs are available, run local smoke and record production smoke as blocked by deploy/domain access.
    - Manual live Google/Yandex OAuth and live OpenRouter+Blob generation checks are marked passed only if actually run.
  </behavior>
  <action>Run the smoke helper and manual/provider checks as available.</action>
  <verify>
    <automated>python backend/scripts/smoke_production.py --api-base-url https://api.comicly.ai --frontend-url https://comicly.ai</automated>
    <manual>Live OAuth and generation verification, only when secrets/domains/providers are configured.</manual>
  </verify>
  <done>Smoke results are captured without overstating unrun external checks.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Create review and verification artifacts</name>
  <files>.planning/phases/08-deployment-and-operations/08-REVIEW.md, .planning/phases/08-deployment-and-operations/08-VERIFICATION.md</files>
  <behavior>
    - Review checks for secret leakage, static exposure, missing env docs, missing rate limits, weak cookies, and unclear deployment gaps.
    - Verification maps OPS-01 through OPS-05 to files, commands, and deploy/smoke status.
    - Any live-production gaps are labeled external/manual, not passed.
  </behavior>
  <action>Create final review and verification docs after local gates and deploy/smoke attempt.</action>
  <verify>
    <manual>Confirm every Phase 8 success criterion has pass/blocker evidence.</manual>
  </verify>
  <done>Phase 8 can be accepted as complete or held with explicit external deployment blockers.</done>
</task>

</tasks>

<verification>
Run from repository root:

```powershell
node --test tests/phase7-static-contract.test.mjs tests/phase8-deploy-config.test.mjs
npm run build:frontend
```

Run from `backend/`:

```powershell
python -m pytest
python -m ruff check .
python -m ruff format --check .
docker compose config
```

Production/preview smoke when URLs exist:

```powershell
python backend/scripts/smoke_production.py --api-base-url https://api.comicly.ai --frontend-url https://comicly.ai
```
</verification>

<success_criteria>
- Local backend/static/deployment gates pass.
- Vercel setup/deploy is attempted through available tools/CLI.
- Production or preview smoke is run when URLs are available.
- Live OAuth/generation checks are marked passed only if actually executed.
- `08-REVIEW.md` and `08-VERIFICATION.md` clearly map OPS-01 through OPS-05 to evidence or external blockers.
</success_criteria>

<output>
After completion, create `.planning/phases/08-deployment-and-operations/08-04-SUMMARY.md`.
</output>
