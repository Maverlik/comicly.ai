---
phase: 03-oauth-sessions-and-profile-bootstrap
plan: 03
type: execute
wave: 3
depends_on:
  - 01
  - 02
files_modified:
  - backend/app/api/v1/me.py
  - backend/app/api/v1/__init__.py
  - backend/app/services/current_user.py
  - backend/app/services/profiles.py
  - backend/tests/test_current_user.py
  - backend/tests/test_me_api.py
autonomous: true
requirements:
  - AUTH-05
  - AUTH-06
  - PROF-01
  - PROF-02
  - TEST-02
must_haves:
  truths:
    - "Private APIs reject anonymous, expired, revoked, or malformed sessions."
    - "`GET /api/v1/me` returns account, profile, and wallet summary from DB."
    - "Display-name update persists and appears in `/api/v1/me`."
    - "Logout revokes the current DB session and clears the product session cookie."
    - "Avatar upload is not implemented; OAuth avatar URL remains read-only bootstrap data."
  artifacts:
    - path: "backend/app/services/current_user.py"
      provides: "Session-cookie-to-current-user dependency"
    - path: "backend/app/api/v1/me.py"
      provides: "Authenticated bootstrap, profile update, and logout endpoints"
    - path: "backend/tests/test_current_user.py"
      provides: "Anonymous/invalid/expired/revoked session coverage"
    - path: "backend/tests/test_me_api.py"
      provides: "`/me`, display-name update, and logout route coverage"
---

<objective>
Add authenticated account/profile/wallet bootstrap APIs and logout.

Purpose: Provide trusted backend account state to future frontend integration while proving anonymous/private API boundaries.
Output: current-user dependency, `/api/v1/me` bootstrap, display-name update, logout, and tests.
</objective>

<context>
@.planning/phases/03-oauth-sessions-and-profile-bootstrap/03-CONTEXT.md
@.planning/phases/03-oauth-sessions-and-profile-bootstrap/03-RESEARCH.md
@.planning/phases/03-oauth-sessions-and-profile-bootstrap/03-01-SUMMARY.md
@.planning/phases/03-oauth-sessions-and-profile-bootstrap/03-02-SUMMARY.md
@backend/BACKEND_TZ.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add current-user dependency</name>
  <files>backend/app/services/current_user.py, backend/tests/test_current_user.py</files>
  <behavior>
    - Missing cookie returns stable auth error envelope.
    - Unknown token hash returns stable auth error envelope.
    - Expired sessions are rejected.
    - Revoked sessions are rejected.
    - Valid sessions resolve the trusted DB user.
  </behavior>
  <action>Create a current-user dependency/service that reads the product session cookie, hashes it, checks `user_sessions`, and loads the associated user. Use `ApiError` with stable machine-readable auth codes.</action>
  <verify>
    <automated>cd backend; python -m pytest tests/test_current_user.py</automated>
  </verify>
  <done>Private API dependencies no longer need to trust browser-supplied identity.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add account/profile/wallet bootstrap endpoint</name>
  <files>backend/app/api/v1/me.py, backend/app/services/profiles.py, backend/tests/test_me_api.py</files>
  <behavior>
    - `GET /api/v1/me` requires authentication.
    - Response includes user id/email/display name/avatar URL, profile fields useful for MVP, and wallet balance.
    - Response reads wallet balance from DB, not from a request payload.
    - Response does not expose session token hashes or provider secrets.
  </behavior>
  <action>Add `/api/v1/me` route and profile read service. Keep response shape small and stable for Phase 7 frontend integration.</action>
  <verify>
    <automated>cd backend; python -m pytest tests/test_me_api.py</automated>
  </verify>
  <done>Authenticated bootstrap API satisfies PROF-01 and gives later frontend work a trusted contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add display-name update and logout</name>
  <files>backend/app/api/v1/me.py, backend/app/services/profiles.py, backend/tests/test_me_api.py</files>
  <behavior>
    - Authenticated user can update display name.
    - Updated display name persists and is returned by `/api/v1/me`.
    - Logout revokes the current session row and clears the product session cookie.
    - Logout does not revoke all devices unless explicitly implemented and tested as current-session-only behavior.
  </behavior>
  <action>Add display-name update and logout endpoints. Keep profile upload/avatar file handling out of scope; do not add storage dependencies.</action>
  <verify>
    <automated>cd backend; python -m pytest tests/test_current_user.py tests/test_me_api.py</automated>
  </verify>
  <done>Phase 3 account/profile/logout API behavior is available without frontend integration.</done>
</task>

</tasks>

<verification>
Run from `backend/`:

```powershell
python -m pytest tests/test_current_user.py tests/test_me_api.py
python -m ruff check .
python -m ruff format --check .
```
</verification>

<success_criteria>
- Authenticated bootstrap, display-name update, and logout exist under `/api/v1`.
- Anonymous/private API behavior is rejected through the stable error envelope.
- Wallet summary comes from DB.
- Avatar upload and frontend integration remain deferred.
</success_criteria>

<output>
After completion, create `.planning/phases/03-oauth-sessions-and-profile-bootstrap/03-03-SUMMARY.md`.
</output>
