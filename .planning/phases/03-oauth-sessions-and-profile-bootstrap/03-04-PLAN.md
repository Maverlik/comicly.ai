---
phase: 03-oauth-sessions-and-profile-bootstrap
plan: 04
type: execute
wave: 4
depends_on:
  - 01
  - 02
  - 03
files_modified:
  - backend/README.md
  - backend/.env.example
  - backend/tests/test_auth_config.py
  - backend/tests/test_oauth_routes.py
  - backend/tests/test_current_user.py
  - backend/tests/test_me_api.py
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - AUTH-04
  - AUTH-05
  - AUTH-06
  - AUTH-07
  - PROF-01
  - PROF-02
  - TEST-02
must_haves:
  truths:
    - "Backend docs explain OAuth env vars, callback URLs, cookie settings, and local test strategy."
    - "Full backend pytest and ruff gates pass."
    - "Phase 3 remains backend API-only with no frontend/root runtime file changes."
    - "Provider secrets, session secrets, and token hashes are not exposed in responses or docs."
  artifacts:
    - path: "backend/README.md"
      provides: "Phase 3 auth setup and verification docs"
    - path: "backend/.env.example"
      provides: "OAuth/session env example without real secrets"
    - path: "backend/tests/*"
      provides: "Complete Phase 3 auth/session test coverage"
---

<objective>
Finalize Phase 3 docs, env examples, and full quality gates.

Purpose: Make the OAuth/session foundation understandable and verifiable before moving to wallet ledger work.
Output: updated backend documentation, env example, full test/lint/format pass, and phase boundary checks.
</objective>

<context>
@.planning/phases/03-oauth-sessions-and-profile-bootstrap/03-CONTEXT.md
@.planning/phases/03-oauth-sessions-and-profile-bootstrap/03-RESEARCH.md
@.planning/phases/03-oauth-sessions-and-profile-bootstrap/03-VALIDATION.md
@.planning/phases/03-oauth-sessions-and-profile-bootstrap/03-01-SUMMARY.md
@.planning/phases/03-oauth-sessions-and-profile-bootstrap/03-02-SUMMARY.md
@.planning/phases/03-oauth-sessions-and-profile-bootstrap/03-03-SUMMARY.md
@backend/BACKEND_TZ.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Document OAuth/session configuration</name>
  <files>backend/README.md, backend/.env.example</files>
  <behavior>
    - Docs list Google/Yandex env vars and callback URL shape.
    - Docs explain successful login redirects to configured creator URL.
    - Docs explain temporary OAuth state session vs DB-backed product session.
    - Docs state avatar file upload is deferred.
  </behavior>
  <action>Update backend docs and env example for Phase 3 OAuth/session setup. Include local and production notes without real secrets.</action>
  <verify>
    <automated>cd backend; python -m pytest tests/test_auth_config.py</automated>
  </verify>
  <done>Developers can configure local/prod OAuth without reading implementation code.</done>
</task>

<task type="auto">
  <name>Task 2: Run full Phase 3 quality gates</name>
  <files>backend/tests/test_auth_config.py, backend/tests/test_oauth_routes.py, backend/tests/test_auth_bootstrap.py, backend/tests/test_current_user.py, backend/tests/test_me_api.py</files>
  <behavior>
    - Full pytest suite passes.
    - Ruff lint passes.
    - Ruff format check passes.
    - No frontend/root runtime files are modified.
  </behavior>
  <action>Run all backend quality gates, fix only backend files if failures are caused by Phase 3 changes, and confirm the root/frontend boundary remains untouched.</action>
  <verify>
    <automated>cd backend; python -m pytest && python -m ruff check . && python -m ruff format --check .</automated>
  </verify>
  <done>Phase 3 backend is ready for code review and verification.</done>
</task>

</tasks>

<verification>
Run from `backend/`:

```powershell
python -m pytest
python -m ruff check .
python -m ruff format --check .
```

Boundary check from repository root:

```powershell
git diff --name-only -- server.js package.json index.html create.html scripts styles.css creator.css .env.example
```
</verification>

<success_criteria>
- OAuth/session docs and env examples are accurate.
- All Phase 3 tests and full backend gates pass.
- No frontend/root runtime files are changed.
- Phase 4 can build wallet ledger behavior on top of authenticated users.
</success_criteria>

<output>
After completion, create `.planning/phases/03-oauth-sessions-and-profile-bootstrap/03-04-SUMMARY.md`.
</output>
