---
phase: 03-oauth-sessions-and-profile-bootstrap
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/requirements.txt
  - backend/requirements-runtime.txt
  - backend/app/core/config.py
  - backend/app/main.py
  - backend/app/services/auth_sessions.py
  - backend/tests/test_auth_config.py
  - backend/tests/test_auth_sessions.py
  - backend/.env.example
autonomous: true
requirements:
  - AUTH-07
  - TEST-02
must_haves:
  truths:
    - "Phase 3 auth config exists without requiring provider secrets at app import time."
    - "Product sessions use opaque random tokens and store only hashes in `user_sessions`."
    - "Cookie settings support local development and production `.comicly.ai` deployment."
    - "Authlib temporary OAuth state session is separate from the product session cookie."
  artifacts:
    - path: "backend/app/core/config.py"
      provides: "OAuth, redirect, cookie, and session lifetime settings"
    - path: "backend/app/services/auth_sessions.py"
      provides: "Product session token, hash, cookie, and validation helpers"
    - path: "backend/app/main.py"
      provides: "Temporary OAuth state middleware and CORS credentials setup"
    - path: "backend/tests/test_auth_sessions.py"
      provides: "Session hash/cookie helper coverage"
---

<objective>
Create the Phase 3 auth configuration and product-session primitives.

Purpose: Prepare secure settings, dependencies, middleware, and tests before adding provider-specific OAuth callbacks or private APIs.
Output: Authlib dependency, env settings, temporary OAuth state middleware, CORS credential config, session token/hash helpers, and tests.
</objective>

<context>
@.planning/phases/03-oauth-sessions-and-profile-bootstrap/03-CONTEXT.md
@.planning/phases/03-oauth-sessions-and-profile-bootstrap/03-RESEARCH.md
@.planning/phases/03-oauth-sessions-and-profile-bootstrap/03-VALIDATION.md
@backend/BACKEND_TZ.md

<locked_decisions>
- Backend remains API-only and frontend/root files must not be modified.
- Product sessions are DB-backed opaque tokens, not JWTs.
- Session lifetime is 30 days.
- Production login success redirects to `https://comicly.ai/create.html`, configurable by env.
- Avatar upload is out of scope.
</locked_decisions>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add auth dependencies and settings</name>
  <files>backend/requirements.txt, backend/requirements-runtime.txt, backend/app/core/config.py, backend/.env.example, backend/tests/test_auth_config.py</files>
  <behavior>
    - `authlib` is available in runtime and test installs.
    - Settings include `SESSION_SECRET`, frontend redirect URL, OAuth provider env names, session cookie name/domain/secure/samesite, and 30-day lifetime.
    - Missing OAuth provider secrets do not prevent app import or non-auth tests.
    - `.env.example` documents all Phase 3 env vars without real values.
  </behavior>
  <action>Add Authlib to requirements and extend settings/env docs for OAuth/session/cookie configuration. Keep provider secrets optional at import time and validated only when provider routes need them.</action>
  <verify>
    <automated>cd backend; python -m pytest tests/test_auth_config.py</automated>
  </verify>
  <done>Auth settings are centralized, documented, and covered by tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add product session token and cookie helpers</name>
  <files>backend/app/services/auth_sessions.py, backend/tests/test_auth_sessions.py</files>
  <behavior>
    - Session tokens are cryptographically random opaque values.
    - Only a hash of the token is intended for database storage.
    - Cookie helper can set and clear the product session cookie with configured flags.
    - Helper behavior is deterministic enough for tests without weakening runtime randomness.
  </behavior>
  <action>Create session helper functions/classes for token generation, token hashing, expiry calculation, and response cookie set/clear behavior. Do not create login routes in this task.</action>
  <verify>
    <automated>cd backend; python -m pytest tests/test_auth_sessions.py</automated>
  </verify>
  <done>Session helpers support the later OAuth callback and logout plans safely.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add temporary OAuth state middleware and credential CORS</name>
  <files>backend/app/main.py, backend/app/core/config.py, backend/tests/test_auth_config.py</files>
  <behavior>
    - App adds Starlette `SessionMiddleware` only for temporary OAuth state handling.
    - Product session remains the opaque DB-backed cookie from `auth_sessions.py`.
    - App enables credentialed CORS only for configured origins.
    - Wildcard CORS origins are not used with credentials.
  </behavior>
  <action>Wire temporary OAuth state middleware and CORS into `create_app()` from settings. Document in tests that this middleware is not the product session store.</action>
  <verify>
    <automated>cd backend; python -m pytest tests/test_auth_config.py tests/test_auth_sessions.py</automated>
  </verify>
  <done>App-level auth plumbing is ready for OAuth routes without weakening product session design.</done>
</task>

</tasks>

<verification>
Run from `backend/`:

```powershell
python -m pytest tests/test_auth_config.py tests/test_auth_sessions.py
python -m ruff check .
python -m ruff format --check .
```
</verification>

<success_criteria>
- Auth settings and product session helpers exist and are tested.
- Authlib/state middleware is available but product sessions remain DB-backed.
- Cookie/CORS config is production-safe and configurable.
- No frontend/root runtime files are changed.
</success_criteria>

<output>
After completion, create `.planning/phases/03-oauth-sessions-and-profile-bootstrap/03-01-SUMMARY.md`.
</output>
