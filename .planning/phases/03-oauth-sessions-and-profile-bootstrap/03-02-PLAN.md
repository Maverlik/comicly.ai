---
phase: 03-oauth-sessions-and-profile-bootstrap
plan: 02
type: execute
wave: 2
depends_on:
  - 01
files_modified:
  - backend/app/api/v1/__init__.py
  - backend/app/api/v1/auth.py
  - backend/app/services/oauth_providers.py
  - backend/app/services/auth_bootstrap.py
  - backend/tests/test_oauth_routes.py
  - backend/tests/test_auth_bootstrap.py
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - AUTH-04
  - TEST-02
must_haves:
  truths:
    - "Google and Yandex login routes start backend-owned OAuth redirects."
    - "Callbacks validate provider responses server-side through provider service abstractions."
    - "Returning users attach by `(provider, provider_user_id)` first."
    - "Verified-email provider linking attaches Google/Yandex identities to one account."
    - "Unverified or missing email does not silently merge accounts."
  artifacts:
    - path: "backend/app/api/v1/auth.py"
      provides: "OAuth login/callback route surface"
    - path: "backend/app/services/oauth_providers.py"
      provides: "Provider profile normalization and Authlib client access"
    - path: "backend/app/services/auth_bootstrap.py"
      provides: "User/provider/profile/wallet bootstrap service"
    - path: "backend/tests/test_oauth_routes.py"
      provides: "Mocked provider route tests"
    - path: "backend/tests/test_auth_bootstrap.py"
      provides: "Provider linking and first-login bootstrap tests"
---

<objective>
Add backend-owned Google/Yandex OAuth routes and provider identity bootstrap.

Purpose: Create users and link provider identities from trusted server-side OAuth callbacks without touching the frontend.
Output: `/api/v1/auth/...` login/callback routes, provider normalization, first-login bootstrap service, and mocked OAuth tests.
</objective>

<context>
@.planning/phases/03-oauth-sessions-and-profile-bootstrap/03-CONTEXT.md
@.planning/phases/03-oauth-sessions-and-profile-bootstrap/03-RESEARCH.md
@.planning/phases/03-oauth-sessions-and-profile-bootstrap/03-01-SUMMARY.md
@backend/BACKEND_TZ.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add OAuth provider normalization and clients</name>
  <files>backend/app/services/oauth_providers.py, backend/tests/test_oauth_routes.py</files>
  <behavior>
    - Provider profiles normalize to provider, provider_user_id, email, email_verified, display_name, and avatar_url.
    - Google verified email supports email-linking.
    - Yandex supports provider-id login and only email-linking when the normalized profile marks email as verified/trusted.
    - Provider secrets are required only when constructing live provider clients.
  </behavior>
  <action>Create provider config/profile normalization helpers and test them with fixture payloads. Keep live network calls behind service boundaries so automated tests can use fakes.</action>
  <verify>
    <automated>cd backend; python -m pytest tests/test_oauth_routes.py</automated>
  </verify>
  <done>Provider responses can be normalized safely without live OAuth tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add first-login bootstrap and verified-email linking</name>
  <files>backend/app/services/auth_bootstrap.py, backend/tests/test_auth_bootstrap.py</files>
  <behavior>
    - First login creates user, provider identity, user profile, wallet, and starter grant transaction.
    - Starter grant uses `Settings.starter_coins` and a stable idempotency key.
    - Returning provider login reuses the existing user.
    - Second provider login with same verified email links to the existing user.
    - Unverified/missing email creates or uses provider-specific account only.
  </behavior>
  <action>Implement transaction-scoped bootstrap service. Use the Phase 2 SQLAlchemy models and existing wallet transaction constraints; do not implement general wallet credit/debit services beyond the starter grant.</action>
  <verify>
    <automated>cd backend; python -m pytest tests/test_auth_bootstrap.py</automated>
  </verify>
  <done>OAuth identity bootstrap satisfies AUTH-03/AUTH-04 without Phase 4 wallet scope creep.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add OAuth login and callback routes</name>
  <files>backend/app/api/v1/auth.py, backend/app/api/v1/__init__.py, backend/tests/test_oauth_routes.py</files>
  <behavior>
    - `GET /api/v1/auth/google/login` and `GET /api/v1/auth/yandex/login` initiate OAuth redirects.
    - Provider callbacks call provider validation/normalization and bootstrap services server-side.
    - Successful callback creates a product session cookie and redirects to configured creator URL.
    - Provider errors return or redirect through a stable, non-secret error path.
  </behavior>
  <action>Add auth router and include it under `/api/v1`. Use fake provider/bootstrap dependencies in tests to avoid live Google/Yandex calls. Keep route naming stable and documented in test expectations.</action>
  <verify>
    <automated>cd backend; python -m pytest tests/test_oauth_routes.py tests/test_auth_bootstrap.py</automated>
  </verify>
  <done>OAuth routes are testable, backend-owned, and produce DB-backed product sessions after callbacks.</done>
</task>

</tasks>

<verification>
Run from `backend/`:

```powershell
python -m pytest tests/test_oauth_routes.py tests/test_auth_bootstrap.py
python -m ruff check .
python -m ruff format --check .
```
</verification>

<success_criteria>
- Google/Yandex OAuth route surface exists under `/api/v1/auth`.
- First login and provider linking behavior are covered with fixtures.
- Successful callback can create a product session and redirect to creator URL.
- No live provider credentials are required for automated tests.
</success_criteria>

<output>
After completion, create `.planning/phases/03-oauth-sessions-and-profile-bootstrap/03-02-SUMMARY.md`.
</output>
