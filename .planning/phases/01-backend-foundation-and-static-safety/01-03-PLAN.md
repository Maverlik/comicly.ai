---
phase: 01-backend-foundation-and-static-safety
plan: 03
type: execute
wave: 3
depends_on:
  - 01
  - 02
files_modified:
  - backend/tests/conftest.py
  - backend/tests/test_health.py
  - backend/tests/test_static_safety.py
  - backend/tests/test_config.py
  - backend/tests/test_errors.py
  - backend/pyproject.toml
  - backend/pytest.ini
  - backend/README.md
  - backend/docs/phase1-boundary.md
autonomous: true
requirements:
  - SAFE-01
  - SAFE-03
  - SAFE-04
  - SAFE-05
  - OPS-06
  - TEST-01
must_haves:
  truths:
    - "Automated tests prove the FastAPI backend does not serve repository-root files, frontend files, dotfiles, planning files, backend docs, or traversal paths."
    - "Automated tests prove `/health`, `/ready`, config loading, and error envelopes work as Phase 1 contracts."
    - "Quality commands for pytest, Ruff lint, and Ruff format check are documented and pass from `backend/`."
    - "Phase 1 preserves existing root AI route contracts by leaving the root Node runtime untouched and documenting the migration boundary."
  artifacts:
    - path: "backend/tests/test_static_safety.py"
      provides: "Representative deny tests for private/root/static paths"
    - path: "backend/tests/test_health.py"
      provides: "Health/readiness endpoint behavior tests"
    - path: "backend/tests/test_config.py"
      provides: "Config safe-default tests"
    - path: "backend/tests/test_errors.py"
      provides: "Stable error envelope and validation tests"
    - path: "backend/pyproject.toml"
      provides: "Ruff lint/format configuration"
    - path: "backend/README.md"
      provides: "Backend local, Docker, test, lint, and migration commands"
    - path: "backend/docs/phase1-boundary.md"
      provides: "Backend-only scope and AI route migration boundary"
  key_links:
    - from: "backend/tests/conftest.py"
      to: "backend/app/main.py"
      via: "FastAPI app fixture"
      pattern: "create_app"
    - from: "backend/tests/test_static_safety.py"
      to: "backend/app/main.py"
      via: "HTTPX ASGI client"
      pattern: "AsyncClient|ASGITransport"
    - from: "backend/README.md"
      to: "backend/docker-compose.yml"
      via: "documented startup command"
      pattern: "docker compose up"
---

<objective>
Add the Phase 1 automated safety net, quality gates, and backend documentation.

Purpose: Prove static safety, production-safe health/readiness, stable error handling, config behavior, and legacy-route preservation boundaries before later phases add trusted data and business logic.
Output: Pytest suite, Ruff config, pytest config, backend README, and Phase 1 boundary documentation.
</objective>

<execution_context>
@C:/Users/ivan3/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/ivan3/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-backend-foundation-and-static-safety/01-CONTEXT.md
@.planning/phases/01-backend-foundation-and-static-safety/01-RESEARCH.md
@.planning/phases/01-backend-foundation-and-static-safety/01-VALIDATION.md
@.planning/phases/01-backend-foundation-and-static-safety/01-01-SUMMARY.md
@.planning/phases/01-backend-foundation-and-static-safety/01-02-SUMMARY.md
@backend/BACKEND_TZ.md

<locked_decisions>
- Keep every runtime change inside `backend/` per D-01 through D-04.
- Static safety for this standalone API backend means no root static mount, no implicit file server behavior, and tests proving representative private paths return 404 per D-23/D-24.
- Use Pytest, HTTPX/FastAPI test patterns, and Ruff per D-26 through D-30.
- Do not implement OAuth, wallet, comic persistence, OpenRouter generation, payments, or frontend integration in tests or docs as Phase 1 runtime behavior.
</locked_decisions>

<interfaces>
Use contracts created by Plans 01 and 02:

```python
from app.main import create_app
from app.core.config import Settings
from app.core.errors import ApiError
from app.db.health import check_database_ready
```

Test client pattern:

```python
import pytest
from httpx import ASGITransport, AsyncClient

@pytest.mark.asyncio
async def test_health(async_client: AsyncClient) -> None:
    response = await async_client.get("/health")
    assert response.status_code == 200
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add API, config, and error tests</name>
  <files>backend/tests/conftest.py, backend/tests/test_health.py, backend/tests/test_config.py, backend/tests/test_errors.py, backend/pytest.ini</files>
  <behavior>
    - `/health` returns 200 and minimal JSON without `database_url`, `api_key`, `model`, `OPENROUTER`, or secret-like values.
    - `/ready` returns 200 when the DB readiness helper is overridden to succeed.
    - `/ready` returns 503 with `{ "error": { "code": "DATABASE_UNAVAILABLE", ... } }` when the DB readiness helper raises `ApiError`.
    - Config tests prove Phase 1 app startup does not require deferred OAuth, OpenRouter, wallet, payment, or storage env vars.
    - Validation or explicit error tests prove stable JSON error shape with machine-readable codes.
  </behavior>
  <action>Create pytest infrastructure under `backend/tests`. Use HTTPX ASGI transport against `create_app()` per D-27. Add dependency override or monkeypatch pattern for readiness so tests can verify success and failure without requiring Docker Postgres for unit-like API tests. Add `pytest.ini` with `pythonpath = .` and async settings if needed. Keep tests backend-only and do not import root `server.js` or frontend files.</action>
  <verify>
    <automated>cd backend; python -m pytest tests/test_health.py tests/test_config.py tests/test_errors.py</automated>
  </verify>
  <done>Health/readiness, config, validation, and error envelope tests exist and pass from `backend/`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add static safety and migration-boundary tests/docs</name>
  <files>backend/tests/test_static_safety.py, backend/docs/phase1-boundary.md</files>
  <behavior>
    - Requests to `/.env`, `/.planning/PROJECT.md`, `/backend/BACKEND_TZ.md`, `/package.json`, `/index.html`, `/create.html`, `/scripts/creator.js`, and traversal attempts return 404 or another non-success safe response from the FastAPI backend.
    - The backend has no `StaticFiles` mount and no file-serving route.
    - Documentation states `GET /api/health`, `POST /api/ai-text`, and `POST /api/generate-comic-page` remain owned by the existing root Node runtime during Phase 1 because root/frontend files are intentionally untouched per D-01 through D-05.
    - Documentation states future migration of AI routes requires a later phase with frontend/root change approval.
  </behavior>
  <action>Add static safety tests for representative private/root/frontend paths and encoded/traversal-style paths per SAFE-01 and TEST-01. These tests should assert the new FastAPI backend is API-only, not that the old root server is fixed in this phase. Add `backend/docs/phase1-boundary.md` documenting the scope decision for SAFE-05: Phase 1 preserves current AI route contracts by not changing `server.js` or frontend/root files; OpenRouter generation migration is deferred and requires a later approved plan.</action>
  <verify>
    <automated>cd backend; python -m pytest tests/test_static_safety.py</automated>
  </verify>
  <done>Static/private path tests pass, and the Phase 1 boundary document explains legacy AI route preservation without implementing or modifying those routes.</done>
</task>

<task type="auto">
  <name>Task 3: Add Ruff config, README, and full quality gate</name>
  <files>backend/pyproject.toml, backend/README.md</files>
  <action>Add `backend/pyproject.toml` with Ruff lint and format configuration suitable for Python 3.12. Add `backend/README.md` with commands for local virtualenv setup, dependency install, running Uvicorn, Docker Compose startup, Pytest, Ruff check, Ruff format check, Ruff format, Alembic current/revision/upgrade commands, and env variable meanings. Document that backend port defaults to 8000, Postgres is managed by backend Docker Compose, `/health` and `/ready` are unprefixed, and future business routes belong under `/api/v1/`. Include a root-change guard command that fails if runtime files outside `backend/` changed after running from `backend/`: `git -C .. diff --name-only -- server.js package.json index.html create.html scripts styles.css creator.css .env.example` should produce no output after execution.</action>
  <verify>
    <automated>cd backend; python -m pytest; python -m ruff check .; python -m ruff format --check .</automated>
  </verify>
  <done>All backend tests, lint, and format checks pass; README contains local, Docker, migration, quality, and scope-boundary commands.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP client to API-only backend | Anonymous clients can request arbitrary paths. |
| test fixtures to app dependencies | Tests override readiness behavior and must not hide production route contracts. |
| developer docs to operations | README commands influence how secrets, Docker, and migrations are handled locally. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | I | `backend/tests/test_static_safety.py` | mitigate | Assert private/root/frontend paths and traversal attempts are not served by the FastAPI backend. |
| T-03-02 | R | `backend/tests/test_errors.py` | mitigate | Assert stable machine-readable error codes so future API failures are auditable and testable. |
| T-03-03 | I | `backend/README.md` | mitigate | Document env handling without including secret values; point to `.env.example` only. |
| T-03-04 | T | `backend/docs/phase1-boundary.md` | mitigate | Explicitly preserve root AI route ownership and require later approval for frontend/root runtime changes. |
| T-03-05 | D | `backend/tests/test_health.py` | mitigate | Verify `/health` remains DB-independent and `/ready` has controlled dependency failure behavior. |
</threat_model>

<verification>
Run after Plans 01 and 02 are complete:

```powershell
cd backend
python -m pip install -r requirements.txt
python -m pytest
python -m ruff check .
python -m ruff format --check .
git -C .. diff --name-only -- server.js package.json index.html create.html scripts styles.css creator.css .env.example
```

The final `git diff --name-only` command must print no runtime-root/frontend file paths. If it prints any path, stop and ask the user whether to approve that out-of-backend change per D-04 before continuing.
</verification>

<success_criteria>
- Only `backend/` files are created or modified by this plan.
- Automated tests cover static safety, traversal/dotfile denial, health/readiness, config behavior, API error format, and validation behavior.
- `SAFE-05` is covered by preserving existing root Node AI contracts untouched and documenting that migration is deferred to later approved phases.
- `python -m pytest`, `python -m ruff check .`, and `python -m ruff format --check .` pass from `backend/`.
- README documents local setup, Docker startup, quality gates, migration commands, env variables, and Phase 1 backend-only boundaries.
</success_criteria>

<output>
After completion, create `.planning/phases/01-backend-foundation-and-static-safety/01-03-SUMMARY.md`.
</output>
