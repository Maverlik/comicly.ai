---
phase: 05-private-comic-persistence
plan: 04
type: execute
wave: 4
depends_on:
  - 01
  - 02
  - 03
files_modified:
  - backend/README.md
  - backend/tests/test_comic_service.py
  - backend/tests/test_comics_api.py
autonomous: true
requirements:
  - COMIC-01
  - COMIC-02
  - COMIC-03
  - COMIC-04
  - COMIC-05
  - COMIC-06
  - TEST-04
must_haves:
  truths:
    - "Backend docs explain private comic persistence endpoints and boundaries."
    - "Full backend pytest and ruff gates pass."
    - "Phase 5 remains backend API-only with no frontend/root runtime file changes."
    - "Docs do not imply real generation, wallet debit, or object storage is implemented."
  artifacts:
    - path: "backend/README.md"
      provides: "Phase 5 API and boundary documentation"
    - path: "backend/tests/test_comic_service.py"
      provides: "Service coverage for draft reload and owner scope"
    - path: "backend/tests/test_comics_api.py"
      provides: "API coverage for draft reload and owner scope"
---

<objective>
Finalize Phase 5 docs, quality gates, and backend-only verification.

Purpose: Make private comic persistence understandable and fully verified before generation integration begins in Phase 6.
Output: docs update, full backend gates, and boundary confirmation.
</objective>

<context>
@.planning/phases/05-private-comic-persistence/05-CONTEXT.md
@.planning/phases/05-private-comic-persistence/05-RESEARCH.md
@.planning/phases/05-private-comic-persistence/05-VALIDATION.md
@.planning/phases/05-private-comic-persistence/05-01-SUMMARY.md
@.planning/phases/05-private-comic-persistence/05-02-SUMMARY.md
@.planning/phases/05-private-comic-persistence/05-03-SUMMARY.md
@backend/BACKEND_TZ.md
</context>

<threat_model>
- **Threat:** Documentation encourages unsafe client-trusted owner ids or generation behavior. **Mitigation:** Document authenticated current-user ownership and Phase 6 boundary clearly.
- **Threat:** Regression in auth/wallet/schema behavior from comic changes. **Mitigation:** Run full backend pytest and ruff gates.
- **Threat:** Accidental frontend/root edits. **Mitigation:** Run explicit boundary diff check and stage only approved files.
</threat_model>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Document private comic persistence</name>
  <files>backend/README.md</files>
  <behavior>
    - Docs list new private comic endpoints.
    - Docs explain compact list vs full detail.
    - Docs explain soft archive and last-write-wins.
    - Docs explicitly say generation, wallet debit, object storage, and frontend integration remain later phases.
  </behavior>
  <action>Update backend README with concise Phase 5 API and boundary notes.</action>
  <verify>
    <manual>Read docs against `05-CONTEXT.md` and `backend/BACKEND_TZ.md`.</manual>
  </verify>
  <done>Future agents and developers can understand the Phase 5 API without reading all implementation code.</done>
</task>

<task type="auto">
  <name>Task 2: Run full backend gates and boundary check</name>
  <files>backend/tests/test_comic_service.py, backend/tests/test_comics_api.py</files>
  <behavior>
    - Full pytest suite passes.
    - Ruff lint passes.
    - Ruff format check passes.
    - No frontend/root runtime files are modified.
  </behavior>
  <action>Run full backend quality gates, fix only backend files if failures are caused by Phase 5 changes, and confirm frontend/root boundary remains untouched.</action>
  <verify>
    <automated>cd backend; python -m pytest && python -m ruff check . && python -m ruff format --check .</automated>
    <automated>git diff --name-only -- server.js package.json index.html create.html scripts styles.css creator.css .env.example</automated>
  </verify>
  <done>Phase 5 is ready for code review and verification.</done>
</task>

</tasks>

<verification>
Run from `backend/`:

```powershell
python -m pytest
python -m ruff check .
python -m ruff format --check .
```

Boundary check from repository root:

```powershell
git diff --name-only -- server.js package.json index.html create.html scripts styles.css creator.css .env.example
```
</verification>

<success_criteria>
- Private comic persistence docs are accurate.
- All Phase 5 tests and full backend gates pass.
- No frontend/root runtime files are changed.
- Phase 6 can connect generation jobs to persisted comics/pages.
</success_criteria>

<output>
After completion, create `.planning/phases/05-private-comic-persistence/05-04-SUMMARY.md`.
</output>

